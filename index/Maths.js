'use strict';


const add = (a, b) => a + b;

const subtract = (a, b) => a - b;

const div = (a, b) => a / b;

const multiply = (a, b) => a * b;

module.exports = {  //Need to do this to import in other files
    add,
    subtract,
    multiply,
    div
}